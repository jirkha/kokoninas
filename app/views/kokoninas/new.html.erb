<%# This view is separated from the form partial to allow for custom layout, headings, or instructions. %>
<%# It is rendered by the 'new' action in KokoninasController. %>
<h2 class="text-2xl font-bold text-slate-700 mb-6">VLOŽIT NOVOU KOKONINU</h2>

<%# The render method includes the _form partial, passing the @kokonina object. %>
<%# This keeps the form code DRY and reusable for new records. %>
<%= render "form", kokonina: @kokonina %>